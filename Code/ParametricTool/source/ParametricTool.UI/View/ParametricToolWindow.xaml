<Window x:Class="ParametricTool.UI.View.ParametricToolWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:vm="clr-namespace:ParametricTool.UI.ViewModels"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ParametricTool.UI.View"
        mc:Ignorable="d"
        Title="参数化工具" Height="750" Width="1000" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.DataContext>
        <vm:ParametricToolViewModel />
    </Window.DataContext>
    <hc:Interaction.Triggers>
        <hc:EventTrigger EventName="ContentRendered">
            <hc:InvokeCommandAction Command="{Binding InitCommand}" />
        </hc:EventTrigger>
    </hc:Interaction.Triggers>
    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="160"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="120"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <TextBox Grid.Row="0" Grid.Column="0" Margin="0,5,0,5" Text="{Binding TemplateModel}"></TextBox>
            <TextBox Grid.Row="1" Grid.Column="0" Margin="0,5,0,5" Text="{Binding TemplateDrawing}"></TextBox>
            <TextBox Grid.Row="2" Grid.Column="0" Margin="0,5,0,5" Text="{Binding InstanceExcel}"></TextBox>
            <TextBox Grid.Row="3" Grid.Column="0" Margin="0,5,0,5" Text="{Binding OutputPath}"></TextBox>
            <Button HorizontalAlignment="Right" Width="110" Grid.Row="0" Grid.Column="1" Content="选择模板模型" Margin="0,5,0,5" Command="{Binding SelectTemplateModelCommand}" Style="{StaticResource ButtonPrimary}"></Button>
            <Button HorizontalAlignment="Right" Width="110" Grid.Row="1" Grid.Column="1" Content="选择模板工程图" Margin="0,5,0,5" Command="{Binding SelectTemplateDrawingCommand}" Style="{StaticResource ButtonPrimary}"></Button>
            <Button HorizontalAlignment="Right" Width="110" Grid.Row="2" Grid.Column="1" Content="选择实例表格" Margin="0,5,0,5" Command="{Binding SelectInstanceExcelCommand}" Style="{StaticResource ButtonPrimary}"></Button>
            <Button HorizontalAlignment="Right" Width="110" Grid.Row="3" Grid.Column="1" Content="选择输出路径" Margin="0,5,0,5" Command="{Binding SelectOutputPathCommand}" Style="{StaticResource ButtonPrimary}"></Button>
        </Grid>
        <Grid Grid.Row="1">
            <DataGrid ItemsSource="{Binding ModelItemCollection.Items}" x:Name="m_DataGrid_ModelItems" CanUserAddRows="False" CanUserSortColumns="False" AutoGenerateColumns="False" hc:DataGridAttach.CanUnselectAllWithBlankArea="False" SelectionMode="Single" HeadersVisibility="Column" >
            </DataGrid>
        </Grid>
        <Grid Grid.Row="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="测试" Command="{Binding TestCommand}" Margin="0,0,10,0" Style="{StaticResource ButtonPrimary}"></Button>
                <Button Content="生成实例" Command="{Binding ApplyCommand}" Margin="0,0,10,0" Style="{StaticResource ButtonPrimary}"></Button>
                <Button Content="取消" Command="{Binding CancleCommand}" Style="{StaticResource ButtonDefault}"></Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

